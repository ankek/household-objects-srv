GET /api/v1/auth/device-tokens	covered-by: TestTenantIsolation/(cookie|bearer)/device_tokens_list -- group A's device token id is absent from group B's listing
POST /api/v1/auth/logout	covered-by: internal/httpapi/logout_test.go's TestLogoutRevokesOnlyTheSessionBehindTheCallersOwnCookie plus internal/storage's TestRevokeCallingSessionRejectsAnotherGroupsDigest -- this route takes NO id in its path, no body and no query parameter: the session cookie is simultaneously the credential AND the address of the thing it ends, so there is nothing in the request a caller could name wrongly and neither the cross-group-404 shape nor the collection-absence shape applies to it. The FORMAT section's THIRD (structural) shape is the closest fit and is stronger here than for the id-less CREATE routes it was written for: logoutHandler passes identity.Group and identity.UserID -- both read from the request's OWN authenticated context, never from anything a caller supplies -- into SessionAdmin.RevokeCallingSession, whose statement binds group_id, user_id AND token_hash. A request cannot express "end a session belonging to someone else" at all. The storage-layer test named above is the runtime half (household B presenting household A's digest leaves A's session live), and TestRevokeCallingSessionRejectsASameGroupDifferentUsersDigest closes the tenant->user level P-3's group scoping cannot see, exactly as DELETE /auth/sessions/{sessionID} already does. Read queries/sessions.sql's RevokeSessionByTokenHash doc before trusting the attribution: three of its four predicates fire under mutation, and group_id is honestly recorded there as MASKED by user_id (a user belongs to exactly one group, so no fixture this schema can hold makes the two independently observable) -- the route's tenant behaviour is proven, the credit to one predicate is not claimed. The cookie-only gate is proven separately by TestLogoutRejectsABearerOnlyRequest, against a config whose general authenticator accepts every bearer request so a 401 can only come from the narrower gate
POST /api/v1/auth/device-tokens	covered-by: TestUnscopedConstructionDoesNotCompile (T030, server/tests/tenant_isolation/compilefail_test.go) -- deviceTokenIssueHandler passes GroupID: identity.Group, the authenticated scope, never a request field, so P-3's group-bound repositories (T026) enforce this route's tenant property structurally and the compile-fail suite proves an unscoped construction does not build; belt-and-braces runtime check: TestTenantIsolation/cookie/device_tokens_create_readback -- create as group B, the new device token is present in B's own listing and absent from A's (cookie-only, per bearer/device_tokens_issue_is_cookie_only, so no bearer variant)
DELETE /api/v1/auth/device-tokens/{deviceTokenID}	covered-by: TestTenantIsolation/(cookie|bearer)/device_tokens_revoke -- 404 (never 403) on group A's id, and the id is not echoed in the body
POST /api/v1/auth/login	exempt: unauthenticated (public) -- no tenant to cross
GET /api/v1/auth/me	covered-by: internal/httpapi/authme_test.go's TestAuthMeHandlerReturnsCallerIdentity plus tests/integration/auth_me_test.go's TestAuthMeAtHTTPLayer -- this route takes NO id in its path, no body and no query parameter, and returns ONLY the fields already carried by the request's own authenticated [middleware.Identity] (group, user id, role) plus a username looked up in that SAME identity's own group roster (scope.Members().List, already bound to the caller's own group by middleware.Scoped/P-3): there is nothing in the request a caller could name wrongly, so neither the cross-group-404 shape nor the collection-absence shape applies to it. The FORMAT section's THIRD (structural) shape is the closest fit, the identical posture POST /api/v1/auth/logout's own line above takes for the identical reason (a route addressed entirely by the caller's own authenticated context, not by anything the request supplies): authMeHandler passes identity.Group/identity.UserID -- both read from the request's OWN authenticated context, never from anything a caller supplies -- into the response, and the username lookup runs over scope.Members(), which P-3's group-bound repositories (T026) already restrict to that same group; a caller cannot express "tell me someone else's identity" at all, in either the URL or the body. TestAuthMeHandlerReturnsCallerIdentity is the unit-layer proof that a MEMBER identity gets the member's own username/role back, not the group owner's (the same distinguishing move TestSessionsListHandlerReturnsOnlyTheCallersOwnSessions makes for its own sibling route); TestAuthMeAtHTTPLayer is the real-router, real-database belt-and-braces half, authenticating as two genuinely different households' users (alice the owner, bob a redeemed member) over both credential kinds and requiring each caller's own GET /auth/me response to match that SAME caller's own POST /auth/login response exactly
POST /api/v1/auth/register	exempt: unauthenticated (public) -- no tenant to cross
GET /api/v1/auth/sessions	covered-by: TestTenantIsolation/(cookie|bearer)/sessions_list -- group A's session id is absent from group B's listing
DELETE /api/v1/auth/sessions/{sessionID}	covered-by: TestTenantIsolation/(cookie|bearer)/sessions_revoke -- 404 (never 403) on group A's id, and the id is not echoed in the body
GET /api/v1/custom-field-defs	covered-by: TestTenantIsolation/(cookie|bearer)/custom_field_defs_list -- group A's definition id is absent from group B's listing, which holds exactly B's own one definition (an empty listing would satisfy an exclusion check vacuously, the identical argument items_list's own coverage line makes); this is the FORMAT section's SECOND assertion shape (collection-absence), not the cross-group-404 shape every /items/{itemID}/... route uses, because there is no {id} in this path at all -- the resource is the caller's own group's definitions, not one addressed row
POST /api/v1/custom-field-defs	covered-by: TestUnscopedConstructionDoesNotCompile (T030, server/tests/tenant_isolation/compilefail_test.go) -- customFieldDefCreateHandler passes middleware.Scoped's storage.Scope straight to customfields.CreateCustomFieldDef, and storage.CustomFieldDefRepository declares no method that takes a group identifier at all (storage/custom_field_defs.go), so a caller cannot name the group it writes to; P-3's group-bound repositories (T026) enforce this route's tenant property structurally, and mutation testing confirmed the mechanism directly: removing the group_id VALUES column from CreateCustomFieldDef's INSERT does not merely fail a test, it does not compile (`unknown field GroupID in struct literal of type gen.CreateCustomFieldDefParams`); belt-and-braces runtime check: TestTenantIsolation/(cookie|bearer)/custom_field_defs_create_readback -- create as group B, the new definition is present in B's own listing and absent from A's. This route is owner-gated (middleware.RequireOwner, A101.1); household B's registering user is an owner of its own group, so this is a tenant boundary and not a role check, the identical fact invites_create_readback's own coverage line states
DELETE /api/v1/custom-field-defs/{fieldDefID}	covered-by: TestTenantIsolation/(cookie|bearer)/custom_field_defs_delete -- 404 (never 403) on group A's id, and the id is not echoed in the body; this is the definitions probe with NO scoped pre-read in front of its statement, so DeleteCustomFieldDef's own group_id/id predicates are the only thing enforcing the boundary. Breaking the group_id predicate ALONE is directly observable: the probe answers 204 and TestTenantIsolation/group_a_resources_untouched, which re-reads A's definition afterwards and requires it still readable at version 1, then finds it gone. Breaking the id predicate ALONE is ALSO directly observable here (unlike T056's item_id predicate, which this HTTP suite could not see at all) -- but the failure it produces is a SAME-TENANT self-inflicted wipe, not a cross-tenant leak: household B's own DELETE against A's real id, correctly scoped to B's OWN group_id, matches every live row B's group has (B holds exactly one), so the mutated statement tombstones B's OWN definition and answers 204 instead of 404, while A's row is left completely untouched. See the STATUS AS OF T057 section above, and storage/custom_field_defs_test.go's TestCustomFieldDefDeleteRemovesExactlyOneRow, for the storage-layer test that isolates the id predicate from that coincidence and is the one test anywhere in this suite that fails under that mutation for the right reason
PUT /api/v1/custom-field-defs/{fieldDefID}	covered-by: TestTenantIsolation/(cookie|bearer)/custom_field_defs_update -- 404 (never 403, and never the 409 a version conflict would give) on group A's id, sent with a VALID body (a real field_type, non-empty name, version 1) so the 404 is the tenant boundary rather than the handler's own body validation, and the id is not echoed back; TestTenantIsolation/group_a_resources_untouched additionally re-reads A's definition afterwards and requires its name and version 1 unchanged. NOTE on where this route's enforcement actually lives: breaking UpdateCustomFieldDef's own group_id predicate alone does NOT change any result, because customFieldDefRepository.Update opens with a scoped GetCustomFieldDef that answers 404 for a foreign group before the UPDATE is issued; breaking the pre-read's group_id too lands the write for real (TestCustomFieldDefUpdateRejectsAnotherGroupsRow observes a nil error), which is what shows the predicate is load-bearing rather than dead. Breaking GetCustomFieldDef's group_id predicate ALONE (the pre-read itself) fires directly through BOTH Get and this route: it leaks A's row to a same-mutation Get, and Update's own UPDATE statement (unmutated) then matches 0 rows against the pre-read's wrongly-scoped version, surfacing as a 500 rather than a silent write -- confirmed live at `PUT /custom-field-defs/{id} -> 500`
GET /api/v1/groups/detail-visibility	covered-by: TestTenantIsolation/(cookie|bearer)/groups_visibility_get -- 200 with a real version (not the collection-route cross-group-404 shape: there is no {id} in this path to name wrong, per the FORMAT section's second assertion shape); the isolation claim proper is carried by group_a_resources_untouched below, which is the only check that would catch this route's sibling (PUT) leaking a write into another tenant's row
PUT /api/v1/groups/detail-visibility	covered-by: TestTenantIsolation/(cookie|bearer)/groups_visibility_update -- household B (an owner of its OWN group, so a 403 here would be the wrong failure, same as invites_list below) successfully toggles its own visibility state twice, once per credential kind; TestTenantIsolation/group_a_resources_untouched re-reads household A's visibility state afterwards, using A's own cookie, and requires it exactly as provisionTenant left it (all three flags false, version 1) -- the only check that would catch UpdateGroupVisibility's `id = ?` predicate breaking and B's own-group write landing on A's row as well as (or instead of) B's, since SQLite applies an UPDATE with no tenant predicate to every row a bare `version = ?` still matches
GET /api/v1/groups/members	covered-by: TestTenantIsolation/(cookie|bearer)/members_list -- household B's own roster (username, role, joined-at) contains EXACTLY household B's one registering user (household A's own username is absent, and an empty listing would satisfy that exclusion vacuously -- the same non-vacuity argument invites_list/labels_list make for their own collections); this is the FORMAT section's SECOND assertion shape (collection-absence), the identical shape groups_visibility_get's own line states for the identical reason -- there is no {id} in this path to name wrong, the resource is the caller's own group's membership. Member-readable (A115, not owner-gated like invites_list below): household B's registering user IS an owner of its own group throughout this suite, so this loop alone cannot observe the READ-side role claim either way (a 403 would be indistinguishable from a correctly-scoped 200 that merely happened to answer wrong); TestMemberListIsReadableByAMember (internal/httpapi) is what proves it, authenticating as a genuine non-owner member via identityByHeaderAuth, the identical seam TestGroupVisibilityGetIsReadableByAMember/TestCustomFieldDefListIsReadableByAMember use for their own inverse claim. Storage-layer half: storage/members_test.go's TestMemberListIsScopedToItsOwnGroup, the identical shape TestGroupVisibilityGetIsScopedToItsOwnGroup proves for its own group-singleton resource
GET /api/v1/invites	covered-by: TestTenantIsolation/(cookie|bearer)/invites_list -- group A's invite id is absent from group B's listing (caller is an owner, so a 403 would be the wrong failure)
POST /api/v1/invites	covered-by: TestUnscopedConstructionDoesNotCompile (T030, server/tests/tenant_isolation/compilefail_test.go) -- inviteCreateHandler passes GroupID: identity.Group, the authenticated scope, never a request field (a caller cannot name the group it writes to), so P-3's group-bound repositories (T026) enforce this route's tenant property structurally and the compile-fail suite proves an unscoped construction does not build; belt-and-braces runtime check: TestTenantIsolation/(cookie|bearer)/invites_create_readback -- create as group B, the new invite is present in B's own listing and absent from A's
POST /api/v1/invites/redeem	exempt: unauthenticated (public) -- no tenant to cross
DELETE /api/v1/invites/{inviteID}	covered-by: TestTenantIsolation/(cookie|bearer)/invites_revoke -- 404 (never 403) on group A's id; caller is an owner of their OWN group, so this is a tenant boundary and not a role check
GET /api/v1/items	covered-by: TestTenantIsolation/(cookie|bearer)/items_list -- group A's item id is absent from group B's listing, which holds exactly B's own one item (an empty listing would satisfy an exclusion check vacuously) G4 UPDATE: this route now accepts search (`q`), location (`location_id`, `descendants`) and label (`label_id`, repeatable) filters, and its read moved from the generated ListItems to storage.itemRepository.ListFiltered. That matters here because ListFiltered is the FIRST read path in the codebase whose SQL is HAND-WRITTEN rather than generated, so queries/scoping_test.go's lint -- which reads .sql files -- cannot see its tenant predicate at all. sqlc can express neither half: the FTS5 MATCH does not parse in the form that works and silently narrows to one column in the form that compiles, and sqlc.slice() splices bare `?` placeholders into an otherwise-numbered statement, so a filter naming two or more ids binds the WRONG VALUES while compiling and running perfectly (caught here only because every multi-element filter test returned an empty list). Standing in for the lint: storage/items_filter_test.go's TestEveryHandWrittenStatementIsGroupScoped, which enumerates the builder's output for EVERY filter combination and requires each to carry a parameter-bound group_id predicate, a deleted_at filter, and exactly as many bound arguments as placeholders; and TestFilteredQueryBindsTheGroupToEveryTenantTable, which additionally requires the item_labels subquery to bind its OWN group_id rather than inherit scope from the outer query. Behavioural half: TestSearchExcludesAnotherGroupsMatchingItem -- items_fts is instance-wide and carries no group_id at all (it is a derived index, not a tenant table), so both households' rows match a common word INSIDE the index and only the two group predicates keep them apart. Mutation-tested, five for five: dropping items.group_id, dropping il.group_id, appending a placeholder without its argument, dropping label de-duplication, and dropping the search statement's group predicate each fire a named test. Note the last of those fires only the STATIC test -- the filtered statement's own group predicate still excludes the foreign row -- which is the belt-and-braces design working and the reason the static check exists rather than being redundant with the behavioural one. The filter parameters themselves are deliberately NEVER a 400 (a filter naming an unknown id has an empty answer, not an error, so a stale bookmark does not become a broken page) and an unresolvable `descendants=true` subtree falls back to filtering on the named id rather than leaving the filter empty, which would silently WIDEN the request to every location -- internal/httpapi's TestItemListNeverWidensOnAnUnresolvableSubtree T079 UPDATE: this line's own "Behavioural half" only ever drove the search predicate directly at the storage layer; nothing before this task asked the same question through the REAL router for ANY of G4/T077/T078's parameters -- q, location_id, label_id, custom_field, warranty_status, created_from/created_to, updated_from/updated_to, sort. tests/integration/items_filter_tenant_isolation_test.go's TestTenantIsolationItemListFilters (a sibling of TestTenantIsolation with its own two-household fixture, so this task's probes cannot disturb items_list's own exact-one-item invariant) now covers that wire-level gap under both credential kinds: q, location_id, label_id and custom_field each get their own requireIdenticalItemListResponses check -- a query naming a REAL id/value belonging to household A (a's own location, a's own label, a's own "Colour" custom-field name AND value) must answer BYTE-IDENTICAL to one naming nothing at all, the same existence-oracle argument this file's own wantCrossGroupStatus doc makes for a path id, restated here for a filter VALUE; warranty_status is checked twice, once against "active" (which neither household's holder-only warranty block actually satisfies, so this leg only proves exclusion) and once against "any" (which both genuinely do satisfy, the non-vacuous case that proves household B sees its own warrantied item and never household A's); created_from/created_to and updated_from/updated_to are each checked with an all-inclusive bound wide enough to contain every item this fixture could produce, so a broken date predicate would surface household A's item directly; sort=-created_at is checked to confirm ordering never widens the scoped result set; and a malformed custom_field entry (no `:`) and an out-of-set warranty_status value are each checked to resolve to household B's own UNFILTERED list -- not a 500, not an empty list, and never household A's item -- proving the documented "silently ignored" contract (parseItemFilter's and buildWarrantyStatusPredicate's own docs) rather than merely a 200.
POST /api/v1/items	covered-by: TestUnscopedConstructionDoesNotCompile (T030, server/tests/tenant_isolation/compilefail_test.go) -- itemCreateHandler passes middleware.Scoped's storage.Scope straight to items.Create, and storage.ItemRepository declares no method that takes a group identifier (storage/items.go), so a caller cannot name the group it writes to; P-3's group-bound repositories (T026) enforce this route's tenant property structurally and the compile-fail suite proves an unscoped construction does not build; belt-and-braces runtime check: TestTenantIsolation/(cookie|bearer)/items_create_readback -- create as group B, the new item is present in B's own listing, absent from A's listing, and 404 to A by id
GET /api/v1/items/{itemID}	covered-by: TestTenantIsolation/(cookie|bearer)/items_get -- 404 (never 403) on group A's id, and the id is not echoed in the body
PUT /api/v1/items/{itemID}	covered-by: TestTenantIsolation/(cookie|bearer)/items_update -- 404 (never 403, and never the 409 a version conflict would give) on group A's id, sent with a VALID body so the 404 is the tenant boundary rather than the handler's own body validation, and the id is not echoed back; TestTenantIsolation/group_a_resources_untouched additionally re-reads A's item afterwards and requires its name and version 1 unchanged, which is the only check that would catch a 404 answered after the UPDATE already matched
DELETE /api/v1/items/{itemID}	covered-by: TestTenantIsolation/(cookie|bearer)/items_delete -- 404 (never 403) on group A's id, and the id is not echoed in the body; this is the probe with NO scoped pre-read in front of its statement, so DeleteItem's own group_id predicate is the single thing enforcing the boundary. Breaking that predicate ALONE (neutralized to `sqlc.arg(group_id) = sqlc.arg(group_id)`, an always-true tautology that keeps the generated Params struct's GroupID field intact so the caller still compiles), regenerated and re-run live, fires directly and immediately: TestTenantIsolation/cookie/items_delete answers 204 instead of 404 (`carol (cookie) DELETE /api/v1/items/{itemA} -> 204`), and TestTenantIsolation/group_a_resources_untouched independently catches the SAME mutation from A's own side, re-reading A's item afterwards and finding it 404 instead of 200 -- the only check that would catch a 404-shaped answer given AFTER the tombstone (and its whole cascade, see below) already landed. storage/items_delete_test.go's TestItemDeleteRejectsAnotherGroupsItem fails on its own under the identical mutation (`groupA deleting groupB's item = <nil>, want ErrNotFound`), independent of the integration suite. This route is also this suite's single most destructive probe: storage.itemRepository.Delete CASCADES a successful tombstone to the item's warranty/sale/purchase/identifications/custom-fields in the SAME transaction (A107, docs/sdlc/hho-ecosystem/assumptions.md) -- under the SAME group_id mutation, TestTenantIsolation/bearer/items_delete (which runs second, against the SAME shared fixture) answers 404 correctly, because the FIRST (cookie) run's wrongly-permitted DELETE had already consumed A's item; this is expected under this suite's own shared-fixture design (see this file's own doc, "ONE fixture shared by every subtest") and does not weaken the claim -- the cookie run's own 204 and group_a_resources_untouched's own independent re-read are what prove the mutation, not the bearer run's incidental 404. The two-predicate `rows != 1` guard on DeleteItem's own `id` predicate (matching customFieldDefRepository.Delete's choice, not identificationRepository.Delete's/itemCustomFieldRepository.Delete's `rows == 0` -- see gen.Queries.DeleteItem's own SQL doc for the full argument) is a SAME-TENANT hazard this cross-group HTTP suite has no occasion to exercise (household B here only ever owns one item) and is proven instead at the storage layer: storage/items_delete_test.go's fixture seeds TWO items in the SAME group specifically so that a neutralized `id` predicate (matching every live item in the group) is directly observable -- confirmed live, every write-path test in that file fails loudly (`matched 2 rows, want 1`) under that mutation, and a scratch assertion confirmed the SECOND item is left completely untouched (`db.Store.Tx` rolls the whole transaction back on the guard's own returned error, so the multi-row match never actually commits) rather than merely reporting the failure after the fact. The cascade's own per-table predicates (DeleteItemWarranty/DeleteItemSale/DeleteItemPurchase's group_id, reused unmodified from T052/T053/T054's own writes, and CascadeDeleteItemIdentifications'/CascadeDeleteItemCustomFields' group_id/item_id, new to this task) are NOT independently mutation-tested against this route: they run only after DeleteItem's own group_id-scoped tombstone already matched exactly one row in the caller's own group, so a foreign-group DELETE never reaches them at all -- masked by DeleteItem's own gate, the identical shape UpdateItemPurchase's/UpdateItemSale's own group_id predicates have against their pre-read (STATUS AS OF T053/T054 above) -- not measured live for this task, on the reasoning that DeleteItem's own gate structurally prevents the cascade calls from ever running against another tenant's rows in the first place (r.group() is the caller's own scope on every one of those calls, never attacker-controlled, restated in storage/items.go's own Delete doc). The cascade's item_id scoping (a SAME-TENANT hazard, keeping a sibling item's own rows untouched) is proven at the storage layer instead: storage/items_delete_test.go's TestItemDeleteCascadesToDetailBlocks and TestItemDeleteCascadesToIdentificationsAndCustomFields both use a two-item, multi-row fixture (itemA1 being deleted, itemA2 untouched, 3 identifications + 2 custom fields on itemA1 alone) for the identical reason T056's own verification argues a single-row fixture cannot distinguish "the row cascaded" from "every row the group has." CascadeDeleteItemIdentifications' own item_id predicate WAS independently broken and re-run live for this task: neutralized alone, TestItemDeleteCascadesToIdentificationsAndCustomFields fails directly (itemA2's own identification rows wrongly wiped out), while TestTenantIsolation stays fully green under the identical mutation (this HTTP suite never gives one household a second item to leak against) -- confirmed live both ways. CascadeDeleteItemCustomFields' own item_id predicate was NOT independently broken for this task (not measured), on the strength of the identical statement shape and the measurement just made against its sibling table.
DELETE /api/v1/items/{itemID}/custom-fields/{customFieldID}	covered-by: TestTenantIsolation/(cookie|bearer)/item_custom_fields_delete -- 404 (never 403) on group A's item id (naming A's real custom-field row id), and the id is not echoed in the body; this is the probe with NO scoped pre-read in front of its statement, so DeleteItemCustomField's own group_id/item_id/id predicates are the only thing enforcing the boundary. Breaking the group_id predicate ALONE fires directly: the probe answers 204 and TestTenantIsolation/group_a_resources_untouched, which re-reads A's row afterwards and requires it still readable at version 1, then finds it gone (storage/item_custom_fields_test.go's TestItemCustomFieldDeleteRejectsAnotherGroupsRow fails on its own under the identical mutation). Breaking the item_id predicate ALONE (the same-tenant, cross-item hazard this task adds, identical to T056's own) is NOT observable by this route at all -- every cross-group probe here, including this one, stays green under that mutation; it is caught only by storage/item_custom_fields_test.go's TestItemCustomFieldDeleteRejectsAnotherItemsRowInTheSameGroup. Breaking the id predicate ALONE -- the T056/T057 hole, restated on a table with THREE predicates like identifications' own, not custom_field_defs' two -- is ALSO not observable by any cross-group probe: household B's own DELETE, correctly scoped to its own group_id and item_id, matches every LIVE row on B's OWN item (B holds exactly one), so `rows == 0` (this repository's guard, matching identificationRepository.Delete's choice rather than customFieldDefRepository.Delete's `rows != 1` -- see storage/item_custom_fields.go's Delete doc for why THREE predicates ahead of the match bounds the blast radius to one parent item, not every row the group has) reports success and B's own row vanishes; storage/item_custom_fields_test.go's TestItemCustomFieldDeleteRemovesExactlyOneRow (three rows on ONE item) is the one test that isolates this from B's single-row coincidence, and fails with `deleting cf-1 left 0 of 3 custom fields, want 2` under the identical mutation -- confirmed live
GET /api/v1/items/{itemID}/custom-fields	covered-by: TestTenantIsolation/(cookie|bearer)/item_custom_fields_list -- 404 (never 403) on group A's item id, and the id is not echoed in the body; group A really does have a custom-field row (provisionTenant mints one, ad hoc, for both households), so the 404 is the tenant boundary and not a merely-empty list -- this route is the cross-group-404 shape, not the collection-absence shape GET /items uses, because it names an EXISTING parent item going in. List's own group_id predicate is masked by its parent-item pre-read (GetItem, on the UNRELATED `items` table): breaking ListItemCustomFields' own group_id predicate alone does not fire (the pre-read already answers ErrNotFound for a foreign group), confirmed live -- TestTenantIsolation stays green under that single mutation. List's item_id predicate is NOT masked, because nothing else in the same statement re-checks it: breaking it alone leaks itemA2's row into itemA's list at the storage layer (storage/item_custom_fields_test.go's TestItemCustomFieldListExcludesOtherItemsRowsInTheSameGroup fails with `want exactly one row ... itemA2's row leaked`), confirmed live -- but this HTTP suite cannot see it at all (every household here owns exactly one item), so TestTenantIsolation stays green under that mutation too
POST /api/v1/items/{itemID}/custom-fields	covered-by: TestTenantIsolation/(cookie|bearer)/item_custom_fields_create -- 404 (never 403, and never the 500 the composite FOREIGN KEY would give) on group A's item id, sent with a VALID body (field_type "text", a non-empty text_value) so the 404 is the tenant boundary rather than items.ErrCustomFieldValueInvalid's own 400; this create route names a resource id going in, so it is the cross-group-404 shape and not the structural shape POST /items uses; unlike the three detail blocks there is no 409 to rule out, because FR-016 is 0..N; belt-and-braces runtime check: TestTenantIsolation/(cookie|bearer)/item_custom_fields_create_readback -- create an item and a row as group B, read the row back in B's own list, and require 404 to A on the item's list AND on the row's own PUT address. This route is member-writable (router.go mounts it on the general authenticated group, no middleware.RequireOwner) -- the polar opposite of POST /custom-field-defs' owner-gated posture despite both being 0..N collections; household B's registering user is an owner of its own group throughout this suite, so this loop cannot observe the member-writable claim (a 403 would be indistinguishable from a correctly-answered 404) -- that is proven instead by internal/httpapi's own TestItemCustomFieldCreateIsWritableByAMember, authenticating as a genuine, non-owner member
PUT /api/v1/items/{itemID}/custom-fields/{customFieldID}	covered-by: TestTenantIsolation/(cookie|bearer)/item_custom_fields_update -- 404 (never 403, and never the 409 a version conflict would give) on group A's item id (naming A's real custom-field row id), sent with a VALID body (field_type "text", non-empty text_value, version 1) so the 404 is the tenant boundary rather than the handler's own body validation, and the id is not echoed back; TestTenantIsolation/group_a_resources_untouched additionally re-reads A's row afterwards and requires its text_value and version 1 unchanged. Swapping problem.NotFound() for problem.Forbidden() on this handler's storage.ErrNotFound branch fires immediately: TestTenantIsolation/(cookie|bearer)/item_custom_fields_update fails with the harness's dedicated existence-oracle message ("A 403 CONFIRMS the resource exists..."), and internal/httpapi's own TestItemCustomFieldHandlersTranslateStorageErrors/update:_not_found_is_404 catches it too (status = 403, want 404), independent of the integration suite -- confirmed live. THE SAME-TENANT, cross-item hazard T056 first found, restated: this route names TWO ids (itemID and customFieldID), and a child id belonging to a DIFFERENT item in the CALLER's OWN group is a failure mode this HTTP suite cannot exercise at all -- proven instead by storage/item_custom_fields_test.go's TestItemCustomFieldUpdateRejectsAnotherItemsRowInTheSameGroup, which fails under the SAME mutation (item_id predicate on the UPDATE statement itself neutralized) that leaves every subtest on this line green -- confirmed live; that predicate is MASKED by the pre-read's own item_id (breaking the UPDATE statement's copy alone does not fire; breaking the pre-read's copy too lands the write for real, confirmed live by the double mutation). group_id on the UPDATE statement itself is masked identically (breaking it alone does not fire; breaking GetItemCustomField's own group_id too lands the write, confirmed live: the mutated write landed at `PUT .../custom-fields/{id} -> 200` with the pwned text_value, and a repeat run under the second, independent credential answered 409 against the version the first run had already bumped -- both outcomes show the write reached the UPDATE, not that the boundary held)
DELETE /api/v1/items/{itemID}/identifications/{identificationID}	covered-by: TestTenantIsolation/(cookie|bearer)/identifications_delete -- 404 (never 403) on group A's item id (naming A's real identification row id), and the id is not echoed in the body; this is the identifications probe with NO scoped pre-read in front of its statement, so DeleteItemIdentification's own group_id/item_id/id predicates are the only thing enforcing the boundary, and breaking the group_id predicate ALONE is directly observable: the probe answers 204 and TestTenantIsolation/group_a_resources_untouched, which re-reads A's row afterwards and requires it still readable at version 1, then finds it gone. Breaking the item_id predicate ALONE (the same-tenant, cross-item hazard this task adds) is NOT observable by this route at all -- every cross-group probe here, including this one, stays green under that mutation; it is caught only by storage/item_identifications_test.go's TestIdentificationDeleteRejectsAnotherItemsRowInTheSameGroup, per the STATUS AS OF T056 section above
DELETE /api/v1/items/{itemID}/purchase	covered-by: TestTenantIsolation/(cookie|bearer)/purchase_delete -- 404 (never 403) on group A's item id, and the id is not echoed in the body; this is the purchase probe with NO scoped pre-read in front of its statement, so DeleteItemPurchase's own group_id predicate is the single thing enforcing the boundary, and breaking that predicate ALONE is directly observable: the probe answers 204 and TestTenantIsolation/group_a_resources_untouched, which re-reads A's block afterwards and requires it still readable at version 1, then finds it gone -- the only check that would catch a 404 answered after the tombstone already landed; re-applying this mutation against storage/purchase_test.go also makes TestPurchaseDeleteRejectsAnotherGroupsBlock fail on its own
GET /api/v1/items/{itemID}/identifications	covered-by: TestTenantIsolation/(cookie|bearer)/identifications_list -- 404 (never 403) on group A's item id, and the id is not echoed in the body; group A really does have an identification row (provisionTenant mints one for both households), so the 404 is the tenant boundary and not a merely-empty list -- this route is the cross-group-404 shape (FORMAT section's first shape), not the collection-absence shape GET /items uses, because it names an EXISTING parent item going in; List's own pre-read of the parent item is what makes this true, and its own group_id predicate is masked by that pre-read (breaking it alone does not fire; breaking the pre-read too does) exactly as PUT/DELETE below are masked by theirs -- see the STATUS AS OF T056 section above for the full mutation table
GET /api/v1/items/{itemID}/purchase	covered-by: TestTenantIsolation/(cookie|bearer)/purchase_get -- 404 (never 403) on group A's item id, and the id is not echoed in the body; group A really does have a purchase block (provisionTenant mints one for both households and asserts it back), so the 404 is the tenant boundary and not an empty sub-resource; breaking GetItemPurchase's group_id predicate turns this into a 200 carrying A's vendor, so the claim is enforced by that predicate and not by the route happening to have nothing to return
POST /api/v1/items/{itemID}/identifications	covered-by: TestTenantIsolation/(cookie|bearer)/identifications_create -- 404 (never 403, and never the 500 the composite FOREIGN KEY would give) on group A's item id, sent with a VALID body so the 404 is the tenant boundary rather than the handler's own body validation; this create route names a resource id going in, so it is the cross-group-404 shape and not the structural shape POST /items uses; unlike POST .../warranty/sale/purchase there is no 409 to rule out, because FR-015 is 0..N and a create never conflicts with an existing row; belt-and-braces runtime check: TestTenantIsolation/(cookie|bearer)/identifications_create_readback -- create an item and a row as group B, read the row back in B's own list, and require 404 to A on the item's list AND on the row's own PUT address
POST /api/v1/items/{itemID}/purchase	covered-by: TestTenantIsolation/(cookie|bearer)/purchase_create -- 404 (never 403, never the 409 an existing block would give, and never the 500 the composite FOREIGN KEY would give) on group A's item id, sent with a VALID body so the 404 is the tenant boundary rather than the handler's own body validation; this create route names a resource id going in, so it is the cross-group-404 shape and not the structural shape POST /items uses; the 404 rests on an explicit scoped GetItem of the parent item, and removing that guard answers 500 instead -- an existence oracle with extra steps; belt-and-braces runtime check: TestTenantIsolation/(cookie|bearer)/purchase_create_readback -- create an item and a block as group B, read the block back as B, and require 404 to A by the block's only address
PUT /api/v1/items/{itemID}/identifications/{identificationID}	covered-by: TestTenantIsolation/(cookie|bearer)/identifications_update -- 404 (never 403, and never the 409 a version conflict would give) on group A's item id (naming A's real identification row id), sent with a VALID body (a real kind, non-empty value, version 1) so the 404 is the tenant boundary rather than the handler's own body validation, and the id is not echoed back; TestTenantIsolation/group_a_resources_untouched additionally re-reads A's row afterwards and requires its value and version 1 unchanged. THE NEW HAZARD, not exercisable by this HTTP suite: this route names TWO ids (itemID and identificationID), and a child id belonging to a DIFFERENT item in the CALLER's OWN group -- not a different tenant -- is the genuinely new failure mode T056 adds. Every probe in this suite authenticates as a second, independent GROUP, never as a second item within one group, so it cannot observe that hazard at all; it is proven instead by storage/item_identifications_test.go's TestIdentificationUpdateRejectsAnotherItemsRowInTheSameGroup, which fails under the SAME mutation (item_id predicate broken) that leaves every subtest on this line green. See the STATUS AS OF T056 section above for the full mutation table, including which of the group_id/item_id predicates are masked by Update's own pre-read and which are not.
PUT /api/v1/items/{itemID}/purchase	covered-by: TestTenantIsolation/(cookie|bearer)/purchase_update -- 404 (never 403, and never the 409 a version conflict would give) on group A's item id, sent with a VALID body (non-empty vendor, version 1) so the 404 is the tenant boundary rather than the handler's own body validation, and the id is not echoed back; TestTenantIsolation/group_a_resources_untouched additionally re-reads A's block afterwards and requires its vendor and version 1 unchanged. NOTE on where this route's enforcement actually lives: breaking UpdateItemPurchase's group_id predicate alone does NOT change any result, because purchaseRepository.Update opens with a scoped GetItemPurchase that answers 404 for a foreign item before the UPDATE is issued; breaking the pre-read AND the UPDATE together lands the write and group_a_resources_untouched catches it at {vendor:"pwned by carol" version:2}, which is what shows the predicate is load-bearing rather than dead
DELETE /api/v1/items/{itemID}/sale	covered-by: TestTenantIsolation/(cookie|bearer)/sale_delete -- 404 (never 403) on group A's item id, and the id is not echoed in the body; this is the sale probe with NO scoped pre-read in front of its statement, so DeleteItemSale's own group_id predicate is the single thing enforcing the boundary, and breaking that predicate ALONE is directly observable: the probe answers 204 and TestTenantIsolation/group_a_resources_untouched, which re-reads A's block afterwards and requires it still readable at version 1, then finds it gone -- the only check that would catch a 404 answered after the tombstone already landed
GET /api/v1/items/{itemID}/sale	covered-by: TestTenantIsolation/(cookie|bearer)/sale_get -- 404 (never 403) on group A's item id, and the id is not echoed in the body; group A really does have a sale block (provisionTenant mints one for both households and asserts it back), so the 404 is the tenant boundary and not an empty sub-resource; breaking GetItemSale's group_id predicate turns this into a 200 carrying A's buyer_name, so the claim is enforced by that predicate and not by the route happening to have nothing to return
POST /api/v1/items/{itemID}/sale	covered-by: TestTenantIsolation/(cookie|bearer)/sale_create -- 404 (never 403, never the 409 an existing block would give, and never the 500 the composite FOREIGN KEY would give) on group A's item id, sent with a VALID body so the 404 is the tenant boundary rather than the handler's own body validation; this create route names a resource id going in, so it is the cross-group-404 shape and not the structural shape POST /items uses; the 404 rests on an explicit scoped GetItem of the parent item, and removing that guard answers 500 instead -- an existence oracle with extra steps; belt-and-braces runtime check: TestTenantIsolation/(cookie|bearer)/sale_create_readback -- create an item and a block as group B, read the block back as B, and require 404 to A by the block's only address
PUT /api/v1/items/{itemID}/sale	covered-by: TestTenantIsolation/(cookie|bearer)/sale_update -- 404 (never 403, and never the 409 a version conflict would give) on group A's item id, sent with a VALID body (non-empty buyer_name, version 1) so the 404 is the tenant boundary rather than the handler's own body validation, and the id is not echoed back; TestTenantIsolation/group_a_resources_untouched additionally re-reads A's block afterwards and requires its buyer_name and version 1 unchanged. NOTE on where this route's enforcement actually lives: breaking UpdateItemSale's group_id predicate alone does NOT change any result, because saleRepository.Update opens with a scoped GetItemSale that answers 404 for a foreign item before the UPDATE is issued; breaking the pre-read AND the UPDATE together lands the write and group_a_resources_untouched catches it at {buyer_name:"pwned by carol" version:2}, which is what shows the predicate is load-bearing rather than dead
GET /api/v1/items/{itemID}/stock-adjustments	covered-by: TestTenantIsolation/(cookie|bearer)/stock_adjustments_list -- 404 (never 403) on group A's item id, and the id is not echoed in the body; group A really does have a stock-adjustment row (provisionTenant mints one for both households), so the 404 is the tenant boundary and not a merely-empty list -- this route is the cross-group-404 shape, not the collection-absence shape GET /items uses, because it names an EXISTING parent item going in. List's own group_id predicate (ListStockAdjustments' own `group_id = ?`) is masked by its parent-item pre-read (GetItem, the SAME shared `items` table query identifications'/item-custom-fields' own List pre-reads use): neutralizing that predicate ALONE (an always-true tautology, keeping the generated Params struct intact so the caller still compiles), regenerated and re-run live, leaves TestTenantIsolation (every credential, both stock_adjustments_list and stock_adjustments_create) AND every storage/stock_adjustments_test.go test, including TestStockAdjustmentListRejectsAnUnknownOrForeignItem, green -- the pre-read already answers ErrNotFound for a foreign group before the mutated statement is reached, confirmed live rather than assumed from T056/T058's precedent. List's item_id predicate is NOT masked -- nothing else in the same statement re-checks it -- but this HTTP suite cannot observe that at all: neutralizing it ALONE, live, leaves TestTenantIsolation fully green (every household here owns exactly one item, so there is no second item in the SAME group for a cross-item leak to show up against) while storage/stock_adjustments_test.go's TestStockAdjustmentListExcludesOtherItemsRowsInTheSameGroup (a 3-row/2-row, two-item fixture, present in this task's shipped suite from the start) fails outright (`List(itemA) returned 5 rows, want exactly 3 -- itemA2's rows leaked in`) -- the identical masking shape T058's own GET .../custom-fields line documents, confirmed to hold here rather than copied from it. See the STATUS AS OF T059 section above for the full mutation table
POST /api/v1/items/{itemID}/stock-adjustments	covered-by: TestTenantIsolation/(cookie|bearer)/stock_adjustments_create -- 404 (never 403, and never the 500 the composite FOREIGN KEY would give) on group A's item id, sent with a body carrying a signed delta, reason and note; unlike every other create probe in this file there is no BODY VALIDATION 400 this route could be confused with at all -- delta/reason/note carry no CHECK-constraint-backed enumeration or non-empty requirement (stock.go's own doc) -- so "valid" here means only well-formed JSON, not a body shaped to dodge a rejection rule that does not exist. This create route names a resource id going in, so it is the cross-group-404 shape and not the structural shape POST /items uses; unlike the three detail blocks and identifications/item-custom-fields there is no 409 to rule out, because FR-018's history is 0..N and append-only. Belt-and-braces runtime check: TestTenantIsolation/(cookie|bearer)/stock_adjustments_create_readback -- create an item and a row as group B, read the row back in B's own list, and require 404 to A on the item's list; unlike identifications'/item-custom-fields' own readbacks there is no second, row-id-addressed address to probe here at all, because FR-018 has no PUT/DELETE route for one. Create's own tenant enforcement rests entirely on its opening pre-read (GetItem, scoped group_id+item_id, inside the SAME transaction as the write) plus the schema's composite FOREIGN KEY (group_id, item_id) REFERENCES items (group_id, id) as the backstop underneath it -- confirmed live by two mutations, not assumed: neutralizing AdjustItemQuantity's OWN group_id predicate ALONE is masked by that pre-read (TestTenantIsolation, group_a_resources_untouched, and every storage stock-adjustment test all stay green); disabling the pre-read's own tenant check AND that same predicate TOGETHER still does not land a cross-tenant write -- it answers 500 instead (`carol (cookie) POST .../stock-adjustments -> 500`), because the transaction then reaches CreateStockAdjustment's INSERT with group_id = B and item_id = A's real item, no items row exists for that (group_id, item_id) pair, and the FOREIGN KEY itself rejects the INSERT, rolling the whole transaction back -- group_a_resources_untouched still passed clean immediately afterward, confirming A's quantity and history were left untouched by the rollback. This is the identical "existence oracle with extra steps" shape POST .../purchase's/.../sale's own coverage lines document for their explicit scoped GetItem guard, restated here with the schema's FOREIGN KEY as the final backstop. There is no same-tenant, cross-item hazard for this HTTP suite to miss on Create the way it does on GET .../stock-adjustments above: Create takes no second (row) id at all, and its own pre-read is scoped by BOTH group_id and item_id together, so a caller can only ever adjust the exact item it names. See the STATUS AS OF T059 section above for the full mutation table
DELETE /api/v1/items/{itemID}/warranty	covered-by: TestTenantIsolation/(cookie|bearer)/warranty_delete -- 404 (never 403) on group A's item id, and the id is not echoed in the body; this is the probe with NO scoped pre-read in front of its statement, so DeleteItemWarranty's own group_id predicate is the single thing enforcing the boundary, and TestTenantIsolation/group_a_resources_untouched re-reads A's block afterwards and requires it to still be readable with version 1, which is the only check that would catch a 404 answered after the tombstone already landed
GET /api/v1/items/{itemID}/warranty	covered-by: TestTenantIsolation/(cookie|bearer)/warranty_get -- 404 (never 403) on group A's item id, and the id is not echoed in the body; group A really does have a warranty block (provisionTenant mints one for both households), so the 404 is the tenant boundary and not an empty sub-resource
POST /api/v1/items/{itemID}/warranty	covered-by: TestTenantIsolation/(cookie|bearer)/warranty_create -- 404 (never 403, never the 409 an existing block would give, and never the 500 the composite FOREIGN KEY would give) on group A's item id, sent with a VALID body so the 404 is the tenant boundary rather than the handler's own body validation; this create route names a resource id going in, so it is the cross-group-404 shape and not the structural shape POST /items uses; belt-and-braces runtime check: TestTenantIsolation/(cookie|bearer)/warranty_create_readback -- create an item and a block as group B, read the block back as B, and require 404 to A by the block's only address
PUT /api/v1/items/{itemID}/warranty	covered-by: TestTenantIsolation/(cookie|bearer)/warranty_update -- 404 (never 403, and never the 409 a version conflict would give) on group A's item id, sent with a VALID body (non-empty holder, version 1) so the 404 is the tenant boundary rather than the handler's own body validation, and the id is not echoed back; TestTenantIsolation/group_a_resources_untouched additionally re-reads A's block afterwards and requires its holder and version 1 unchanged, which is the only check that would catch a 404 answered after the UPDATE already matched
GET /api/v1/items/{itemID}/labels	covered-by: tests/integration/tenant_isolation_test.go's TestTenantIsolation/(cookie|bearer)/item_labels_list -- 404 (never 403) on group A's item id, and the id is not echoed in the body; group A's item really DOES carry a label (provisionTenant attaches one for both households, asserting the 204 back), so the 404 is the tenant boundary and not an untagged item. This is the cross-group-404 shape, not the collection-absence shape GET /labels uses, because it names an EXISTING parent item going in -- storage.itemLabelRepository.ListForItem pre-reads that item (scoped GetItem) precisely so a foreign id answers ErrNotFound rather than an empty list, which would tell the caller their id was acceptable. Belt-and-braces at the storage layer: storage/item_labels_test.go's TestListForItemRejectsAnotherGroupsItem. The read-time filtering of TOMBSTONED labels (A110 -- a deleted label must vanish from every item's tag list without the delete cascading) is a SAME-TENANT property this cross-group suite has no occasion to exercise, proven instead by TestTombstonedLabelDisappearsFromItsItemsWithoutCascading
PUT /api/v1/items/{itemID}/labels/{labelID}	covered-by: TestTenantIsolation/(cookie|bearer)/item_labels_attach_foreign_item AND .../item_labels_attach_foreign_label -- TWO probes, not one, because this is the only route in the API whose path carries two ids that each need an INDEPENDENT scope check. The first holds the label end legitimate (group B's OWN label) and makes the item end group A's; the second mirrors it (B's own item, A's label). Each answers 404 (never 403, never the 204 a successful attach gives), and neither id is echoed back. Probing only the both-foreign case -- the obvious probe -- would pass with just ONE of the two pre-reads present, since either alone answers 404 when both ids are foreign; that is exactly the hole these paired probes exist to close. Both pre-reads live in storage.itemLabelRepository.Attach and answer the SAME storage.ErrNotFound, so a caller cannot learn which end was at fault. Belt-and-braces at the storage layer: storage/item_labels_test.go's TestAttachRejectsAnotherGroupsItem and TestAttachRejectsAnotherGroupsLabel, the same pairing one layer down. The route's IDEMPOTENCE (a repeat attach is 204, and a re-attach after a detach revives the tombstoned edge rather than inserting a second row) is a same-tenant property proven by TestAttachIsIdempotent and TestReAttachRevivesTheTombstoneRatherThanInsertingASecondRow
DELETE /api/v1/items/{itemID}/labels/{labelID}	covered-by: TestTenantIsolation/(cookie|bearer)/item_labels_detach -- 404 (never 403) on group A's item+label pair, and neither id is echoed in the body; this is the probe with NOTHING in front of it (storage.itemLabelRepository.Detach does NO pre-read), so DeleteItemLabel's own group_id predicate is the single thing standing between this request and household A losing its assignment. TestTenantIsolation/group_a_resources_untouched re-reads A's own item labels afterwards and requires its one label still attached -- the only check that would catch a 404 answered AFTER the tombstone landed. Confirmed live by mutation, not assumed: neutralising DeleteItemLabel's group_id predicate (an always-true tautology, generated Params struct intact) fires storage/item_labels_test.go's TestDetachRejectsAnotherGroupsEdge directly. What this cross-group suite CANNOT observe is the SAME-TENANT blast radius, and this table has TWO of them rather than the one every other 0..N collection has: drop item_id and one detach strips a label off every item in the caller's own group (a COLUMN of the assignment matrix); drop label_id and it strips every label off one item (a ROW). Every household in this suite owns exactly one item and one label, so row, column and cell are the same single edge and all three outcomes are indistinguishable here. Proven instead at the storage layer against a 3x3 fixture -- storage/item_labels_test.go's TestDetachRemovesExactlyOneEdge -- confirmed by mutation to fire on EITHER predicate: with item_id neutralised the detach reports `matched 3 rows, want 1`, and with label_id neutralised likewise. The guard is `rows != 1`, NOT `rows == 0`, for exactly that reason: a bare rows==0 check reads "more than zero rows changed" as success and would pass through both mutations silently
GET /api/v1/labels	covered-by: tests/integration/tenant_isolation_test.go's TestTenantIsolation/(cookie|bearer)/labels_list -- group A's label id is absent from group B's list, and B's list contains EXACTLY B's own one label (not merely "A's id is missing," which an empty listing would satisfy vacuously); belt-and-braces at the storage layer: storage/labels_test.go's TestLabelListExcludesAnotherGroupsRows. This route is member-writable (router.go mounts it on the general authenticated group, no middleware.RequireOwner) and the read side is proven reachable by a genuine member by internal/httpapi's own TestLabelListIsReadableByAMember
POST /api/v1/labels	covered-by: TestUnscopedConstructionDoesNotCompile (T030, server/tests/tenant_isolation/compilefail_test.go) -- labelCreateHandler passes middleware.Scoped's storage.Scope straight to labels.CreateLabel, and storage.LabelRepository declares no method that takes a group identifier at all (storage/labels.go), so a caller cannot name the group it writes to; P-3's group-bound repositories (T026) enforce this route's tenant property structurally. Belt-and-braces RUNTIME half: tests/integration/tenant_isolation_test.go's TestTenantIsolation/(cookie|bearer)/labels_create_readback -- create as group B and require the new label present in B's own listing and absent from A's; storage/labels_test.go's TestLabelCreateAllowsTheSameNameInDifferentGroups proves the identical claim one layer down. This route is member-writable (router.go mounts it on the general authenticated group, no middleware.RequireOwner, the OPPOSITE of /custom-field-defs' owner-gated POST) -- proven by internal/httpapi's own TestLabelCreateIsWritableByAMember, authenticating as a genuine, non-owner member
DELETE /api/v1/labels/{labelID}	covered-by: TestTenantIsolation/(cookie|bearer)/labels_delete -- group B's Delete of group A's real label id answers 404 (never 403), and TestTenantIsolation/group_a_resources_untouched re-reads group A's label list afterwards and requires the row still present with its original name and version unchanged -- the check that would catch a 404 answered AFTER the tombstone already landed. Belt-and-braces at the storage layer: storage/labels_test.go's TestLabelDeleteRejectsAnotherGroupsRow (group B's Delete of group A's real label id answers storage.ErrNotFound, pinned by internal/httpapi's own TestLabelDeleteHandlerReturnsNotFound); this is the probe with NO scoped pre-read in front of its statement, so DeleteLabel's own group_id/id predicates are the only thing enforcing the boundary -- see storage/labels_test.go's TestLabelDeleteRemovesExactlyOneRow (a 3-row fixture) for the id predicate's own mutation-testing proof (rows != 1 guard, matching custom_field_defs' own choice, not identifications'/item-custom-fields' rows == 0), and this task's own MUTATIONS report for the group_id predicate's own runtime confirmation (both the storage-layer test above and TestTenantIsolation/cookie/labels_delete fire when group_id is neutralised in the generated DeleteLabel statement)
PUT /api/v1/labels/{labelID}	covered-by: TestTenantIsolation/(cookie|bearer)/labels_update -- group B's Update of group A's real label id, sent with a valid body (non-empty name, valid color, version 1), answers 404 (never 409, which would confirm the row exists), and TestTenantIsolation/group_a_resources_untouched re-reads group A's label afterwards and requires its name and version unchanged. Belt-and-braces at the storage layer: storage/labels_test.go's TestLabelUpdateRejectsAnotherGroupsRow (answers storage.ErrNotFound, never storage.ErrVersionMismatch, pinned by internal/httpapi's own TestLabelUpdateHandlerReturnsNotFound). NOTE on where this route's enforcement actually lives, restated from custom_field_defs' own identical shape: labelRepository.Update opens with a scoped GetLabel pre-read that answers ErrNotFound for a foreign group before the UPDATE statement (and the A109 name-conflict check) is ever reached, so UpdateLabel's own group_id predicate is masked by that pre-read exactly as UpdateCustomFieldDef's own is -- this task's own MUTATIONS report confirms both halves: neutralising GetLabel's group_id predicate fires TestLabelGetRejectsAnotherGroupsRow and TestLabelUpdateRejectsAnotherGroupsRow, while neutralising UpdateLabel's OWN group_id predicate (pre-read left intact) fires NEITHER -- masked exactly as documented
GET /api/v1/locations	covered-by: TestTenantIsolation/(cookie|bearer)/locations_list -- group A's location id is absent from group B's listing, which holds exactly B's own one location (an empty listing would satisfy an exclusion check vacuously, the identical argument custom_field_defs_list's/items_list's own coverage lines make); this is the FORMAT section's SECOND assertion shape (collection-absence), not the cross-group-404 shape every /items/{itemID}/... route uses, because there is no {id} in this path at all -- the resource is the caller's own group's locations, not one addressed row
POST /api/v1/locations	covered-by: TestUnscopedConstructionDoesNotCompile (T030, server/tests/tenant_isolation/compilefail_test.go) -- locationCreateHandler passes middleware.Scoped's storage.Scope straight to locations.Create, and storage.LocationRepository declares no method that takes a group identifier at all (storage/locations.go), so a caller cannot name the group it writes into; P-3's group-bound repositories (T026) enforce this route's tenant property structurally; belt-and-braces runtime check: TestTenantIsolation/(cookie|bearer)/locations_create_readback -- create as group B, the new location is present in B's own listing and absent from A's. This route is member-writable (router.go mounts it on the general authenticated group, no middleware.RequireOwner) -- household B's registering user is an owner of its own group throughout this suite, so this loop cannot observe the member-writable claim (a 403 would be indistinguishable from a correctly-answered 201) -- that is proven instead by internal/httpapi's own TestLocationCreateIsWritableByAMember (this task), authenticating as a genuine, non-owner member. Body-level rejections (a blank name -> 400; an unknown or cross-group parent_id -> 400, never a 500 from the composite FOREIGN KEY) are proven at the httpapi unit layer, TestLocationHandlersTranslateStorageErrors (this task), not re-proven here -- see the STATUS AS OF T065 section above for why, and for the mutation table showing the FK backstop covers two of the pre-check's three cases but not the tombstoned-parent one
GET /api/v1/locations/tree	covered-by: storage/locations_tree_test.go's TestTreeExcludesAnotherGroupsLocations -- this is the FORMAT section's SECOND assertion shape (collection-absence), not the cross-group-404 shape, because there is no id in this path at all: the resource is the caller's own group's tree. Both households are deliberately given a SAME-NAMED root ("House") so a leak shows up as a duplicate node rather than as an obviously foreign name that a weaker assertion might still have caught, and each household's tree is required to hold EXACTLY its own root with EXACTLY its own counts -- an empty tree would satisfy a bare exclusion check vacuously, the identical argument locations_list's and items_list's own coverage lines make. The counts are the second, subtler leak channel this route has and the one a structural check would miss entirely: a roll-up that summed items without a group predicate would report the correct STRUCTURE while telling household A how many things household B owns, which is why the assertion pins both counts (A sees 1/1, B sees 2/2) rather than only the node set. Both queries behind it are generated and group-scoped (ListLocations, CountLiveItemsPerLocation), so queries/scoping_test.go's lint covers their predicates directly -- unlike GET /api/v1/items, whose filtered read is hand-written. Tombstoned locations are absent and their items counted nowhere (TestTreeExcludesTombstonedLocationsAndTheirItems); a live node whose parent was tombstoned separately is PROMOTED to a root rather than dropped (TestTreePromotesAnOrphanRatherThanDroppingIt) -- a node reachable from no root would be invisible in the one response a UI builds its tree from, and invisible means unrecoverable through the product, a strictly worse outcome than showing it in a slightly wrong place. Routing is pinned separately by internal/httpapi's TestLocationTreeIsNotSwallowedByTheWildcard, whose fake PANICS in Get so a request mis-routed to /locations/{locationID} fails loudly instead of answering a plausible 404
GET /api/v1/locations/{locationID}	covered-by: TestTenantIsolation/(cookie|bearer)/locations_get -- 404 (never 403) on group A's id, and the id is not echoed in the body; this is the probe with NO scoped pre-read in front of its statement (locationRepository.Get IS the read), so its own group_id/id predicates -- proven directly at the storage layer by storage/locations_test.go's TestLocationGetRejectsAnotherGroupsLocation -- are the only thing enforcing the boundary
PUT /api/v1/locations/{locationID}	covered-by: TestTenantIsolation/(cookie|bearer)/locations_update -- 404 (never 403, and never the 400 storage.ErrLocationParentNotFound/storage.ErrLocationCycle would give) on group A's id, sent with a VALID body (non-empty name, version 1, parent_id DELIBERATELY empty so the probe cannot ALSO be rejected by the tree-specific checks and mask the tenant-boundary 404 under test) so the 404 is the tenant boundary rather than the handler's own validation, and the id is not echoed back; TestTenantIsolation/group_a_resources_untouched additionally re-reads A's location afterwards and requires its name and version 1 unchanged. This route's OWN tenant-boundary predicate (locationRepository.Update's opening pre-read, GetLocation scoped group_id+id) is what answers ErrNotFound for a foreign group before the guarded UPDATE is ever reached -- the identical pre-read shape UpdateCustomFieldDef's own coverage line documents. What this probe does NOT exercise: whether household B could re-parent one of ITS OWN locations under household A's location id (a distinct cross-group hazard from editing A's row directly) -- that property is proven instead at the storage layer, storage/locations_test.go's TestLocationUpdateRejectsAnotherGroupsParent and TestLocationCreateRejectsAnotherGroupsParent (Create's own twin), and by the mutation table in the STATUS AS OF T065 section above, which establishes the FK backstop is what actually stops it once the pre-check is removed. The cycle guard (locationMoveCreatesCycle) is a SAME-TENANT hazard this cross-group HTTP suite has no occasion to exercise at all (household B never has access to household A's tree to attempt a same-tree move against) -- proven exclusively at the storage layer, storage/locations_test.go's TestLocationMoveRejectsSelfParent/TestLocationMoveRejectsDeepCycle/TestLocationMoveRejectsCycleThroughATombstonedNode, all three confirmed to fire live under the mutation recorded in the STATUS AS OF T065 section above; the 400 status mapping for both tree-specific errors is proven at the httpapi unit layer, TestLocationHandlersTranslateStorageErrors (this task)
DELETE /api/v1/locations/{locationID}	covered-by: TestTenantIsolation/(cookie|bearer)/locations_delete -- 404 (never 403) on group A's id, and the id is not echoed in the body; this is the probe with NO scoped pre-read in front of its statement, so DeleteLocation's own group_id/id predicates are the only thing standing between this request and household A losing its location -- confirmed live by mutation (STATUS AS OF T065 section above): neutralizing the group_id predicate alone fires DIRECTLY, both at the storage layer (TestLocationDeleteRejectsAnotherGroupsLocation) and through this route (locations_delete answering 204 instead of 404, caught a second time by group_a_resources_untouched re-reading A's location and finding it gone). Unlike identifications'/item-custom-fields' own DELETE there is no THIRD (item_id) predicate behind this guard -- a location's entire address is (group_id, id), the identical shape custom_field_defs_delete's own coverage line documents, so this table's blast radius under a broken id/group_id predicate is every live location the group has, not merely the other rows on one parent item; storage/locations_test.go's TestLocationDeleteRemovesExactlyOneRow is the same-tenant half of that argument (a multi-row, same-group fixture proving the id predicate specifically, the identical shape TestCustomFieldDefDeleteRemovesExactlyOneRow proves for its own table). What this route deliberately does NOT do -- and what group_a_resources_untouched therefore cannot observe either way -- is cascade to live children: storage/locations_test.go's TestLocationDeleteDoesNotCascadeToLiveChildren proves the A108 decision (STATUS AS OF T065 section above) directly, on a same-tenant fixture this cross-group suite has no occasion to construct G2 UPDATE: this route now carries the empty-or-reassign delete-guard, which introduces a 409 that a foreign id must never be able to provoke -- a 409 naming child_count/item_count would confirm both that the id exists AND how much the other household keeps in it, a strictly richer oracle than the 403 NFR-010 already forbids. locationRepository.Delete therefore runs its scoped GetLocation pre-read FIRST, before any count is read, so the tenant boundary answers before the guard is ever consulted; storage/locations_test.go's TestLocationDeleteRefusalPrecedesNothingForAForeignID pins exactly that ordering (household B deleting household A's NON-EMPTY location gets ErrNotFound, never ErrLocationNotEmpty), and internal/httpapi's TestLocationDeleteAnswers404BeforeTheGuard pins the wire half (the 404 body carries neither count nor the guard's problem type). NOTE that this pre-read now MASKS DeleteLocation's own group_id predicate for the cross-group case, exactly as locations_update's coverage line records for UpdateLocation: the mutation that used to fire locations_delete directly now fires the pre-read instead. The reassignment statements are separately mutation-tested and all three fire -- neutralising ReparentLiveChildren's old_parent_id or MoveLiveItemsToLocation's old_location_id fires TestLocationDeleteReassignsChildrenAndItemsToATarget (which carries a deliberate CONTROL branch, a sibling location with its own child and item, precisely so "moved what this location held" and "moved every row in the group" are distinguishable), and neutralising CountLiveChildLocations' deleted_at filter fires TestLocationDeleteIgnoresTombstonedContents
GET /api/v1/status	exempt: unauthenticated (public) -- no tenant to cross
GET /api/v1/openapi.yaml	exempt: unauthenticated (public), same document for every caller -- no tenant to cross (FR-081, D1.9, T094)
GET /api/v1/items/{itemID}/attachments	covered-by: internal/httpapi's own TestAttachmentListRejectsAnUnknownOrForeignItem -- an itemID this route's scope.Attachments().ListForItem (already bound to the caller's OWN group by middleware.Scoped, P-3) reports storage.ErrNotFound for answers 404 with no detail, the identical cross-group-404 shape GET /items/{itemID}/custom-fields' own coverage line documents for its own item-scoped list. This is the basic proof T104a (the task that mounted this route, discovered gathering context for T105's gallery UI) owns. ListForItem's own group_id predicate is masked by its parent-item pre-read (GetItem, on the UNRELATED items table, the identical shape ListItemCustomFields' own coverage line describes): breaking ListAttachmentsForItem's own group_id predicate alone does not fire (the pre-read already answers ErrNotFound for a foreign group). ListForItem's item_id predicate is NOT masked: breaking it alone leaks itemA2's row into itemA's list at the storage layer (storage/attachments_test.go's TestAttachmentListForItemExcludesTombstonedAndOtherItemsRows fails, itemA2's row present in itemA's own result) -- this HTTP suite cannot observe that leak directly (household A owns exactly one item in provisionTenant's own fixture), so the storage-layer test above is this predicate's own proof, matching ListItemCustomFields' identical caveat. Belt-and-braces at the real-database, real-router layer too: TestTenantIsolation/{cookie,bearer}/attachments_list, now present -- it authenticates as household B and requires the same 404 this line's fake-repository proof already gives against household A's real item id, with the id itself never echoed in the body.
POST /api/v1/items/{itemID}/attachments	covered-by: internal/httpapi's own TestAttachmentUploadRejectsAForeignOrUnknownItem -- an ItemID this route's scope.Items() (already bound to the caller's OWN group by middleware.Scoped, P-3) reports storage.ErrNotFound for answers 404 with AttachmentRepository.Create never reached, and the 404 carries no detail. This is the basic proof T084 (the task that mounted this route) owns; the dedicated D1.8 cross-group suite, exercising a REAL database with two real households the way TestTenantIsolation does for every route above, is T089's job, not re-done here. See attachments.Upload's own doc, "Ordering, in full," point 2, for why a foreign-or-unknown item is refused BEFORE a single byte of the upload is read -- the property that makes a cross-group upload attempt write nothing to disk, not merely fail to record a row. storage/attachments_test.go's TestAttachmentCreateRejectsAnItemFromAnotherGroup proves the identical predicate one layer down, against a real on-disk database, for the storage.AttachmentRepository.Create half of this same pre-flight check.
GET /api/v1/items/{itemID}/attachments/{attachmentID}	covered-by: internal/httpapi's own TestAttachmentDownloadRejectsAForeignOrUnknownAttachment -- an attachmentID this route's scope.Attachments().Get (already bound to the caller's OWN group by middleware.Scoped, P-3) reports storage.ErrNotFound for answers 404 with attachments.OpenOriginal NEVER REACHED (no fixture file exists on disk at all for this test, so a 200 could not happen by accident), and the 404 carries no detail. This is the basic proof T086 (the task that mounted this route) owns, matching T084's own identical carve-out for its sibling POST above; the dedicated D1.8 cross-group suite exercising a REAL database is T089's job, not re-done here. Belt-and-braces at the storage layer: storage/attachments_test.go's TestAttachmentGetRejectsAnAttachmentFromAnotherGroup and TestAttachmentGetRejectsAnAttachmentFromAnotherItem prove the identical (group_id, item_id, id) predicate set one layer down, against a real on-disk database, for the storage.AttachmentRepository.Get half of this same pre-flight check -- the read [storage.AttachmentRepository]'s own doc names T086 as the first caller of. Defence-in-depth belt-and-braces below the SQL layer too: internal/attachments's own TestOpenUnderGroupRejectsAStoragePathNamingAnotherGroupsDirectory proves that even a storage_path SOMEHOW naming a different group's directory is refused by the filesystem-level boundary check, independent of the SQL predicate above it. Belt-and-braces at the real-database, real-router layer too: the dedicated D1.8 cross-group suite this line promised is TestTenantIsolation/{cookie,bearer}/attachments_download, now present -- it provisions a REAL uploaded attachment for household A over the real POST route, authenticates as household B, and requires the same 404 this line's fake-repository proof already gives, plus (group_a_resources_untouched) re-downloads household A's own attachment byte-for-byte afterwards to prove no cross-tenant probe elsewhere in the suite silently touched it.
GET /api/v1/items/{itemID}/attachments/{attachmentID}/thumbnail	covered-by: internal/httpapi's own TestAttachmentThumbnailDownloadAnswersNotFoundWhenNoneExists proves the SAME scope.Attachments().Get pre-flight this route shares with its sibling GET (immediately above) refuses an unresolvable attachmentID identically -- both routes are gated by the identical getAttachmentOrNotFound helper (attachments.go), so a group_id/item_id/id predicate broken for one breaks identically for the other; this test additionally exercises the DISTINCT, non-tenant reason this specific route can answer 404 for a REAL, correctly-scoped row (ThumbnailPath NULL -- attachments.ErrNoThumbnail), proving the two 404 causes are deliberately indistinguishable on the wire. The tenant-boundary half proper is therefore already covered by the line directly above; the dedicated D1.8 cross-group suite (T089) is what will exercise this exact route against a real two-household database, not re-done here. Belt-and-braces at the real-database, real-router layer too: the dedicated D1.8 cross-group suite this line promised is TestTenantIsolation/{cookie,bearer}/attachments_thumbnail_download, now present -- household A's provisioned attachment is a real, decodable JPEG (has_thumbnail true), so this probe's 404 is unambiguously the tenant boundary above, never [attachments.ErrNoThumbnail]'s own distinct cause; group_a_resources_untouched re-fetches household A's own thumbnail afterwards and requires 200, disproving a delete-adjacent leak that removed only the thumbnail file.
DELETE /api/v1/items/{itemID}/attachments/{attachmentID}	covered-by: internal/httpapi's own TestAttachmentDeleteRejectsAForeignOrUnknownAttachment -- an attachmentID this route's scope.Attachments().Get (the SAME getAttachmentOrNotFound pre-flight its two sibling GET routes above share, P-3) reports storage.ErrNotFound for answers 404 with scope.Attachments().Delete NEVER REACHED (the fake's own deleteFn is left nil, which panics if called, so a bug that let a tombstone or a file removal happen before the group check completed would fail this test loudly, not merely return the wrong status by coincidence) -- no fixture file is ever unlinked from disk either. This is the basic proof T087 (the task that mounted this route) owns, matching T084/T086's own identical carve-out for their sibling routes above; the dedicated D1.8 cross-group suite exercising a REAL database is T089's job, not re-done here. Belt-and-braces at the storage layer: storage/attachments_test.go's TestAttachmentDeleteRejectsAnAttachmentFromAnotherGroup and TestAttachmentDeleteRejectsAnAttachmentFromAnotherItem prove the identical (group_id, item_id, id) predicate set one layer down, against a real on-disk database, for the storage.AttachmentRepository.Delete half of this same check -- unlike Get/SetThumbnailPath, this method carries NO pre-read of its own (storage/attachments.go's own Delete doc), so those three predicates on DeleteAttachment's own WHERE clause are the ONLY thing enforcing the boundary at that layer. Defence-in-depth belt-and-braces below the SQL layer too: internal/attachments's own TestDeleteRejectsAStoragePathNamingAnotherGroupsDirectory proves that even a storage_path SOMEHOW naming a different group's directory is refused by the filesystem-level boundary check ([removeUnderGroup]/[resolveUnderGroup], download.go) rather than unlinked, independent of the SQL predicate above it. Belt-and-braces at the real-database, real-router layer too: the dedicated D1.8 cross-group suite this line promised is TestTenantIsolation/{cookie,bearer}/attachments_delete, now present -- it authenticates as household B and sends a REAL DELETE against household A's real, uploaded attachment, requiring the same 404 this line's fake-repository proof already gives; group_a_resources_untouched then re-downloads household A's own attachment (as A, not B) and requires the SAME bytes back byte-for-byte, which is the only check that would catch attachments.Delete's own tombstone-plus-unlink landing on disk despite the 404 answered above.

POST /api/v1/sync/pull	covered-by: server/tests/integration's own TestTenantIsolationSyncPullDoesNotLeakAcrossGroups (T155e) -- see "STATUS AS OF T155d" and "CLOSED BY T155e" below for the full argument. Supersedes T155d's own httpapi-unit-level TestSyncPullDoesNotLeakAcrossGroups as the claim this gate cites (that test still exists and still runs, kept as a cheap, always-on tripwire -- see "CLOSED BY T155e" for why it was not deleted).
POST /api/v1/sync/push	covered-by: server/tests/integration's own TestTenantIsolationSyncPushDoesNotLeakAcrossGroups (T164e) -- see "STATUS AS OF T164d" and "CLOSED BY T164e" below for the full argument. Supersedes T164d's own httpapi-unit-level TestSyncPushCreatesItemAndReturnsApplied/TestSyncPushRevealsNothingThatVariesByGroup as the claim this gate cites (those tests still exist and still run, kept as cheap, always-on tripwires -- see "CLOSED BY T164e" for why they were not deleted).

GET /api/v1/backup	cross-tenant-by-design: authenticated (owner-only) but deliberately spans every tenant by design (FR-132) -- see this file's FORMAT section for the full argument, and TestTenantIsolationBackupIncludesEveryGroupsData (server/tests/integration/backup_archive_test.go), a test gate-tenant-isolation.sh's own isolation-suite step actually executes (see this section's own note on why that matters), for the test that makes it falsifiable rather than merely asserted. internal/httpapi/backup_test.go remains this route's HTTP-layer wiring coverage (status codes, headers, stream validity) -- unchanged and still correct, just not what this status token itself certifies.

GET /i/{token}	exempt: this handler never reaches storage on an unauthenticated request (see resolve.go's own file doc, "'No database read' ... is structural, not an optimisation") and, once authenticated, its UUID branch redirects to GET /items/{id} WITHOUT reading the items table at all -- the actual tenant-scoped lookup is that already-covered route's, this handler only ever chooses a URL. Its short-code branch is the one branch that does read storage (storage.ItemRepository.GetByShortCode), but it can never cross groups by construction: the statement's group_id predicate comes from r.group() (the SAME structurally-enforced [middleware.Scoped]-equivalent binding every other repository method in this package uses, never a value this request controls), and short_code carries no cross-tenant ADDRESS to begin with -- two groups' rows sharing a code are simply two independently-keyed rows (ux_items_group_short_code is per-group, not global), never "the same row, wrong owner," so there is no cross-group-404 move this route could even pose. See A123 (docs/sdlc/hho-ecosystem/assumptions.md) for the full design decision and T151e's own note (specs/001-hho-ecosystem/tasks.md) for why the tenant-isolation suite deliberately does not extend to this route.

GET /api/v1/export/items.csv	covered-by: TestTenantIsolation/(cookie|bearer)/export_items_csv -- group A's item id is absent from every data row of group B's exported CSV, which holds exactly one data row (B's own item, matched by id AND by name so a row is not merely counted but confirmed to be B's own) -- an empty export would satisfy the exclusion check vacuously, the identical non-vacuity argument GET /api/v1/items' own line above makes for its JSON counterpart. This route takes no resource id, so it is the FORMAT section's collection-absence shape, not the cross-group-404 shape; it shares GET /items' own seam (middleware.Scoped's storage.Scope) and its own underlying group-scoped read (storage.ItemRepository.List), already proven elsewhere in this file, and additionally reads seven already-covered per-item satellite repositories (labels, identifications, attachments, custom fields, warranty, purchase, sale) whose own group_id predicates are each proven directly by their own covered-by: line above -- this line's own contribution is that the assembled, encoded CSV response never surfaces a foreign group's row.

POST /api/v1/labels/qr/batch	covered-by: TestTenantIsolation/(cookie|bearer)/labels_qr_batch -- group A's item id, named in the same request body as group B's own, is absent from the response's `items` array, while group B's own id IS present (matched by id AND by name) and is the response's ONLY entry -- the non-vacuity argument this route's own by-design foreign/nonexistent-id omission (labels_qr_batch.go, "Foreign and nonexistent ids are indistinguishable, by omission (P-3)") makes essential rather than merely stylistic. This route takes no resource id in its path (ids travel in the request body instead), and its cross-tenant contract poses no cross-group-404 move at all -- a foreign id and a nonexistent one are indistinguishable by design, the identical argument GET /i/{token}'s own exempt: line above makes for its short-code branch -- so this is the FORMAT section's collection-absence shape, restated over a response keyed by request-named ids. It shares GET /items' and GET /export/items.csv's own seam (middleware.Scoped's storage.Scope) but reads a NEW statement, storage.ItemRepository.GetByIDs (T151d), not previously proven by any covered-by: line above.

GET /api/v1/reports/valuation	covered-by: TestTenantIsolation/(cookie|bearer)/reports_valuation -- group_by=label; group A's label id is absent from group B's rows while group B's own label row is present with the correct item_count and total_value_minor computed from provisionReportsFixture's known price.
GET /api/v1/reports/warranty-expiring	covered-by: TestTenantIsolation/(cookie|bearer)/reports_warranty_expiring -- group A's report-fixture item id is absent from group B's rows while group B's own item is present with the fixture's expires_on.
GET /api/v1/reports/purchases	covered-by: TestTenantIsolation/(cookie|bearer)/reports_purchases -- over a [from,to] window bracketing both households' identical purchased_on date, group A's item id and vendor are absent from group B's rows while group B's own purchase row is present intact.
GET /api/v1/reports/item-count-by-location	covered-by: TestTenantIsolation/(cookie|bearer)/reports_item_count_by_location -- group A's location id is absent from group B's rows while group B's own location shows item_count exactly 1 (only the report-fixture item is placed there).
GET /api/v1/export/bom	covered-by: TestTenantIsolation/(cookie|bearer)/export_bom -- item_id, location_id and label_id selection modes each name group A's real id explicitly alongside group B's own in the same request; group A's item is omitted (exactly one data row survives) while group B's own item and its value_minor are present.

POST /api/v1/import/native/upload	covered-by: TestUnscopedConstructionDoesNotCompile (T030, server/tests/tenant_isolation/compilefail_test.go) -- importNativeUploadHandler reaches storage.ImportSessionRepository.Create only through importSessionRepositoryFor's Config.Store.ForGroupImportSessions(scope.GroupID()) construction, and CreateImportSessionParams (storage/import_sessions.go) declares no GroupID field at all, so a caller cannot name the group its session lands in; P-3's group-bound repositories (T026) enforce this route's tenant property structurally and the compile-fail suite proves an unscoped construction does not build. Belt-and-braces runtime check (T148g): TestTenantIsolation/(cookie|bearer)/import_native_upload_symmetry -- upload as group B (via stageOwnExportAsImport), then confirm group A cannot preview or commit that same import_id, mirroring the import_preview/import_commit probes immediately below this route's own that already confirm group B cannot reach group A's upload either; storage/import_sessions_test.go's TestImportSessionCreateIsInvisibleToAnotherGroup proves the identical group_id-scoped INSERT predicate directly, one layer down.

GET /api/v1/import/{importID}/preview	covered-by: TestTenantIsolation/(cookie|bearer)/import_preview (T148g) -- group B, naming group A's real import_id (freshly staged from A's own current export via stageOwnExportAsImport), answers 404 (never 403, and never echoes the id back); TestTenantIsolation/import_group_a_untouched re-reads group A's own session immediately afterward, over the SAME import_id, and requires it still previewable with every row "unchanged" -- a 404 answered after the session was deleted or otherwise mutated would be exactly what that re-read catches. This route's own 404-never-403 collapse (resolveStagedImportSession) already folds an unknown id, an expired session and a foreign group's session into the identical response; storage/import_sessions_test.go's TestImportSessionCreateIsInvisibleToAnotherGroup proves the underlying group_id-scoped SELECT predicate directly, one layer down.

POST /api/v1/import/{importID}/commit	covered-by: TestTenantIsolation/(cookie|bearer)/import_commit (T148g) -- group B, naming group A's real import_id, answers 404 (never 403, never the 409 an already-committed session would give, and never echoes the id back); TestTenantIsolation/import_group_a_untouched then commits group A's own session directly afterward, over the SAME import_id, as a genuine no-op (Summary {Created:0 Updated:0 Unchanged:len(rows)}, no created_items) -- the strongest available proof that none of group B's rejected attempts left a partial write behind, since this exercises the exact same session-lookup/transition statements (resolveStagedImportSession, MarkImportSessionCommitted) a broken group_id predicate would have corrupted. storage/import_sessions_test.go's TestImportSessionCreateIsInvisibleToAnotherGroup proves the underlying group_id-scoped SELECT/UPDATE predicates directly, one layer down; the COMMIT WRITE PATH itself (storage.ImportCommitRepository.Commit) rests entirely on the already-scoped CreateItem/CreateItemLabel/CreateItemIdentification/CreateItemCustomField/CreateItemWarranty/Purchase/Sale and cascade-delete/update statements T051-T060/T067-T068's own covered-by: lines above already prove.
